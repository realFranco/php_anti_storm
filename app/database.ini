APP_DOMAIN=localhost 
APP_PORT=7000

APP_URL=http://${APP_DOMAIN}:${APP_PORT}
APP_ASSETS_ROUTE = /public/assets/

DB_USER=system
DB_PASSWORD=system
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=p2p

DB_CONN=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}
DB_CONN_DEV=postgresql://${DB_HOST}:${DB_PORT}/${DB_DATABASE}
